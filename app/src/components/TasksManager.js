import React, { Component } from 'react';

class TasksManager extends Component {
  render(){
    return(
      <div>
        Connected with Main Component
      </div>
    );
  }
}

export default TasksManager;
